import type { JobOpening } from "@/types/jobs";
type Props={jobs:JobOpening[]};
export function JobListings({jobs}:Props){if(!jobs.length)return <div className="industrial-panel p-8"><h3 className="text-xl font-black uppercase">No positions posted right now.</h3><p className="mt-3 max-w-2xl leading-7 text-white/55">Open positions will appear here when they are entered into the Axe careers system. You can still share your interest using the form below.</p></div>;return <div className="grid gap-4">{jobs.map(job=><article key={job.id} className="industrial-panel p-6 sm:p-8">{job.sample?<p className="mb-5 inline-flex bg-amber-300 px-3 py-2 text-[.65rem] font-black uppercase tracking-[.16em] text-black">Sample Listing — Not an active opening</p>:null}<div className="flex flex-col justify-between gap-5 md:flex-row md:items-end"><div><p className="text-xs font-black uppercase tracking-[.14em] text-[var(--accent)]">{job.area}</p><h3 className="mt-2 text-2xl font-black uppercase sm:text-3xl">{job.title}</h3><p className="mt-2 text-sm text-white/45">{job.location}</p><p className="mt-4 max-w-2xl leading-7 text-white/60">{job.summary}</p></div><span className="shrink-0 text-sm font-black uppercase tracking-[.1em] text-white/35">Development preview</span></div></article>)}</div>}
